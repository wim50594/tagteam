# This file is a TEMPLATE only. Real secrets are generated at deploy time
# by the GitHub Actions workflow (release.yml) from repository secrets/variables
# and applied via `kubectl apply -k overlays/prod` after substitution.
#
# Do NOT commit real secret values here.
apiVersion: v1
kind: Secret
metadata:
  name: tagteams-secrets
  namespace: tagteams
type: Opaque
stringData:
  JWT_SECRET: "CHANGEME_JWT_SECRET"
  ADMIN_PASSWORD: "CHANGEME_ADMIN_PASSWORD"
  POSTGRES_USER: "tagteams"
  POSTGRES_PASSWORD: "CHANGEME_POSTGRES_PASSWORD"
  DATABASE_URL: "postgresql+asyncpg://tagteams:CHANGEME_POSTGRES_PASSWORD@postgres:5432/tagteams"
